# AWS Credentials Example
# Copy this file to ~/.aws/credentials and replace with your actual values
#
# Get your access keys from:
# AWS Console → IAM → Users → terraform-user-wedding-gallery
# → Security credentials → Access keys

# Profile used by Terraform and AWS CLI commands in this repo
[wedding-gallery]
aws_access_key_id = YOUR_TERRAFORM_USER_ACCESS_KEY_ID
aws_secret_access_key = YOUR_TERRAFORM_USER_SECRET_ACCESS_KEY

