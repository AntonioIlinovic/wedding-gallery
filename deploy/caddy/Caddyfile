{
    # Local-only configuration
    acme_ca https://acme-staging-v02.api.letsencrypt.org/directory
    email your-email@example.com
}

http://localhost {
    # Reverse proxy API requests to the backend service
    reverse_proxy /api/* http://backend:8000

    # Reverse proxy all other requests to the frontend service
    reverse_proxy http://frontend:3000
}

# TODO: Add production Caddyfile with real domain and SSL certificate (https://weddinggallery.site)